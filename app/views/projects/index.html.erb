<h1>Projects Index</h1>

<% @projects.each do |p| %>

  <p><%= link_to(p.name, class_project_path(p.id)) %></p>
<% end %>

<%= link_to("New Project", new_project_path) %>
<br>
  <%= link_to("Home", root_path) %>
